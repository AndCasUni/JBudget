package it.unicam.cs.mpgc.jbudget125637.persistency;

public class XMLOperationRepository {
    /**
     * Repository che legge e scrive le operazioni su file (simulato con lista).
     */
  /*  public class FileOperationRepository implements IOOperationRepository {

        private final List<Operation> storage = new ArrayList<>();

        @Override
        public List<Operation> loadAll() {
            return new ArrayList<>(storage); // restituisce copia
        }

        @Override
        public void save(Operation op) {
            storage.add(op);
        }

        @Override
        public void update(Operation op) {
            // logica per aggiornare
        }

        @Override
        public void delete(Operation op) {
            storage.remove(op);
        }
    }*/
}
